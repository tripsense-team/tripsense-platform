spring:
  application:
    name: api-gateway
  data:
    redis:
      url: ${REDIS_URL}
  cloud:
    gateway:
      httpclient:
        response-timeout: ${GATEWAY_HTTP_RESPONSE_TIMEOUT:240s}
      globalcors:
        cors-configurations:
          "[/**]":
            allowedOrigins:
              - "http://localhost:3000"
              - "http://localhost:3001"
              - "http://127.0.0.1:3000"
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true

server:
  port: ${API_GATEWAY_PORT:${GATEWAY_PORT:${SERVER_PORT:8080}}}
  shutdown: graceful

eureka:
  client:
    enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:${DISCOVERY_SERVER_PORT:8761}/eureka/}

  instance:
    prefer-ip-address: true

management:
  health:
    redis:
      enabled: false
  endpoint:
    health:
      probes:
        enabled: true

  endpoints:
    web:
      exposure:
        include: health,info

tripsense:
  gateway:
    places-rate-limit:
      replenish-rate: ${PLACES_RATE_LIMIT_REPLENISH_RATE:10}
      burst-capacity: ${PLACES_RATE_LIMIT_BURST_CAPACITY:20}
    social-rate-limit:
      enabled: ${SOCIAL_RATE_LIMIT_ENABLED:false}
      replenish-rate: ${SOCIAL_RATE_LIMIT_REPLENISH_RATE:30}
      burst-capacity: ${SOCIAL_RATE_LIMIT_BURST_CAPACITY:60}
    chat-rate-limit:
      enabled: ${CHAT_RATE_LIMIT_ENABLED:true}
      replenish-rate: ${CHAT_RATE_LIMIT_REPLENISH_RATE:5}
      burst-capacity: ${CHAT_RATE_LIMIT_BURST_CAPACITY:10}
    ai:
      response-timeout: ${AI_RESPONSE_TIMEOUT:240s}
    client-ip:
      trusted-proxies: ${TRUSTED_PROXY_CIDRS:127.0.0.1/32,::1/128}
